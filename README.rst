mgardf
======
.. image:: https://ci.appveyor.com/api/projects/status/github/metamorph-inc/mgardf?svg=true

This is a library for converting GME MGA files to RDF. By default it translates the user model. If given access to the UDM XML, it can export an RDF version of the language as well.
